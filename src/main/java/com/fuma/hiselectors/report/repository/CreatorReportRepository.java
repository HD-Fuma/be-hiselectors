package com.fuma.hiselectors.report.repository;

import com.fuma.hiselectors.report.model.CreatorReport;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CreatorReportRepository extends JpaRepository<CreatorReport, Long> {
}
